const path = require('path');

module.exports = {
	htmlDir: path.join(process.cwd(), 'src/public/html/'),
	imgDir: path.join(process.cwd(), 'src/public/images/'),
	pdfDir: path.join(process.cwd(), 'src/public/pdf/'),
};
